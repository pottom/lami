description "Laptop"

layers "core" "tools" "gui"     // no rice -- so it gets none of its files

gpu   "nvidia"
ucode "amd"
class "laptop"

cpu_threads 16
ddc   off           // internal panel; brightnessctl handles this instead
